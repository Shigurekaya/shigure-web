---
description: shigure-web 提交必须使用 Shigurekaya 身份，否则 Vercel 不部署
alwaysApply: true
---

# Git 作者身份（强制）

在本仓库（`shigure-web`）创建、提交或推送时：

- **必须**使用：
  - `GIT_AUTHOR_NAME` / `GIT_COMMITTER_NAME` = `榧`
  - `GIT_AUTHOR_EMAIL` / `GIT_COMMITTER_EMAIL` = `163858348+Shigurekaya@users.noreply.github.com`
- 或确认 `git config --local user.name` / `user.email` 已是上述值。
- **禁止**使用其它身份（如 QQ 邮箱、`zzy` 等）提交；Vercel Hobby 会 Block，线上不更新。
- **禁止**修改全局 `git config --global user.*`；只用本仓库 local 或单次环境变量。
- 提交后用 `git log -1 --format='%an <%ae>'` 确认作者再 push。
