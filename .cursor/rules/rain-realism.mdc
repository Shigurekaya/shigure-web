---
description: 大雨/暴雨视觉必须以「最真实」为第一目标
globs: js/*rain*.js,js/glass-drops.js,js/storm-lightning.js,css/style.css,docs/*thunder*,docs/*rain*
alwaysApply: false
---

# 雨效：最真实优先

## 硬性目标

对齐小米天气「**屏幕是玻璃，雨水打在玻璃上**」（用户参考录屏）。真实度 > 性能。

## 参考片深度结论（必读）

对 `d63123f5…mp4`（1220×2656 / ~5.6s）做时间中位数 / 运动分离后：

| 层 | 特征 |
|----|------|
| **背景雨丝** | 短密近竖直；mid 带 streak 高≈11px、宽≈1.5px；光流向下 |
| **贴屏玻璃** | 存在于 median（相对静止）；亮点相对局部 **+≈40 luma**；整屏亮点覆盖仅 **≈1.4%**（透亮点阵，非灰罩） |
| **冷凝/珠** | mid ≈334 圆珠/MP；尺寸约数像素；多数粘附，偶发泪痕下滑 |
| **UI** | 白字对比仍高（p95−p5≈158），玻璃不压死可读性 |
| **湿感梯度** | 下半屏静亮点比例更高 |

产品要求补充：冷凝珠 **最终都会往下流动**（粘附有限时，到期必释放），与「任一帧多数仍粘着」可并存（cling 错开）。

## 开源「最真」路径（已评估）

| 方案 | 观感 | 本站 |
|------|------|------|
| Radiant / Codrops / raindrop-fx：水图 + WebGL 真折射 | 最高 | 需背景纹理；活 UI + html2canvas **手机黑点/灰蒙，禁用** |
| atmos-fx 玻璃卡水珠 | 很真 | 折射预烘焙贴图，非整页活 DOM |
| BigWings / WebGPU raindrop | 很真 | 全屏 shader，折射固定图 |
| **本站 Canvas2D 贴屏** | 对齐参考片分层与高光 | **唯一稳妥盖 UI**：透镜珠 + 活冷凝粒子 + screen |

## 禁止踩坑

- 全屏 raindrop-fx 盖 UI + html2canvas
- 默认 mist + 近黑 mistColor
- WebGL 雨丝 drawImage 进 2D 折射底图
- 大软灰喷雾球；或 `noSpray` 一刀切删冷凝

## 当前实现

1. GPU 短密雨帘（`sheet`）在背景
2. `glass-drops.js`：透心透镜主珠（含 drip 必滑）+ **活冷凝粒子（cling→必流）** + 细泪痕；screen 合成
3. raindrop-fx 仅无 GPU 时背景兜底，`mist: false`

## 验收

- 一眼是「玻璃上的水」，不是滤镜噪点/墨点/灰罩
- 白底头像上透亮珠；文字可读
- 冷凝珠会陆续下滑，不会永久钉死
