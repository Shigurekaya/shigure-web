---
description: 大雨/暴雨视觉必须以「最真实」为第一目标
globs: js/*rain*.js,js/glass-drops.js,js/storm-lightning.js,css/style.css,docs/*thunder*,docs/*rain*
alwaysApply: false
---

# 雨效：最真实优先

## 硬性目标

- 大雨 / 暴雨要对齐 **小米天气「雨水打在玻璃屏上」**（用户参考录屏）：短密近竖直雨丝 + 整屏湿玻璃水珠/泪痕盖在 UI 上。
- **真实度 > 性能**（尤其 `?rain=storm`）。
- 参考片量化特征（须贴近）：微珠约 **2px**、mid 带约 **650 圆珠/MP**、泪痕极细长、贴屏层帧间几乎静止、透亮高光而非墨点/灰罩。

## 开源「最真」路径（已评估）

| 方案 | 观感 | 本站可否直接上 |
|------|------|----------------|
| Radiant / Codrops：水图 + WebGL 折射 | 最真 | 需背景纹理；活 UI 要截图 |
| raindrop-fx + html2canvas 贴 UI | 强折射 | **手机已证伪**（黑点/灰蒙） |
| BigWings / jeantimex raindrop（全屏 shader） | 很真 | 折射的是固定图，不是活 DOM |
| SVG `backdrop-filter` 位移（GlassFX 等） | 真折射 | Chromium 为主；难驱动成百颗动态珠 |
| **本站 Canvas2D 贴屏（当前）** | 对齐参考片观感 | **唯一稳妥且可盖 UI**：透镜珠 + 细亮冷凝 + screen 合成 |

结论：在修好「活 UI 真折射」前，继续强化 `js/glass-drops.js` 对齐参考片，不要回退到全屏 raindrop+截图。

## 禁止再次踩坑

- **不要**全屏 raindrop-fx 盖 UI + html2canvas（手机黑点/灰蒙）。
- **不要**默认 mist + 近黑 mistColor。
- **不要** WebGL 雨丝 `drawImage` 进 2D 折射底图。
- **不要**大软灰喷雾球；**也不要** `noSpray` 一刀切删冷凝。

## 当前可靠做法

1. **背景**：GPU 短密竖直雨帘（`sheet`）+ 冷蓝天空。
2. **贴屏** `glass-drops.js`：透心透镜主珠、~2px 冷凝高光点（screen）、细亮泪痕；手机同步开泪痕与冷凝。
3. **raindrop-fx**：仅无 GPU 时背景兜底，`mist: false`。

## 验收

- 白底头像上是透亮珠/细亮点，不是墨点或脏灰罩；文字可读。
- 对照参考片：湿、密、透，而不是滤镜噪点。
