---
description: 大雨/暴雨视觉必须以「最真实」为第一目标
globs: js/*rain*.js,js/glass-drops.js,js/storm-lightning.js,css/style.css,docs/*thunder*,docs/*rain*
alwaysApply: false
---

# 雨效：最真实优先

## 硬性目标

对齐小米天气「雨水打在玻璃屏上」。真实度 > 性能，但 **文字必须可读**。

## 已证伪（勿再默认开启）

**全屏 `raindrop-fx` + `html2canvas` 盖 UI**——即使修了双 WebGL / 忽略 canvas / mist:false：

- 本质是把活 UI **整页换成截图底图再折射**，手机上整体发暗、发糊、对比度崩（2026-08-21 实机截图 mean≈51、字难读）
- 「过黑探测」拦不住这种「灰暗糊玻璃」，不是纯黑点问题
- **默认 `useScreenGlass = false`**；代码可留作实验，勿再当上线路径

其它禁令：默认 mist 近黑；WebGL 雨丝 drawImage 进 2D 底图；大软灰喷雾球。

## 当前上线路径

1. GPU / 2D 短密雨丝在 `site-bg`（正文下方）
2. `glass-drops.js` 清晰透镜珠 + 活冷凝（cling→下滑）盖在 `site-fx`，**活 DOM 保持锐利**
3. 背景雾 CSS 保持很淡，勿压死字
4. raindrop-fx 仅无 GPU 时作 **背景层** 兜底（不盖正文）

若以后要「珠内文字真弯折」：优先研究 Chromium `backdrop-filter` + 位移图（活采样），且必须手机验证对比度，再考虑合入。

## 验收

- 头像/标题清晰可读，不是暗糊一层膜
- 水珠透亮有高光，会下滑
- 无整屏黑点 / 灰罩
