---
description: 大雨/暴雨视觉必须以「最真实」为第一目标
globs: js/*rain*.js,js/glass-drops.js,js/storm-lightning.js,css/style.css,docs/*thunder*,docs/*rain*
alwaysApply: false
---

# 雨效：最真实优先

## 硬性目标

对齐小米天气「**屏幕是玻璃，雨水打在玻璃上**」（用户参考录屏）。真实度 > 性能。
文字穿过水珠应有折射/轻微发糊（真折射底图像素），不是平面贴纸。

## 参考片深度结论

| 层 | 特征 |
|----|------|
| **背景雨丝** | 短密近竖直 |
| **贴屏玻璃** | 透亮高光珠 + 泪痕盖 UI；亮点约 +40 luma；覆盖约 1.4% |
| **冷凝** | 粘附后最终下滑（cling 错开） |
| **UI** | 可读；珠内文字可弯折/发糊 |

## 贴屏真折射（raindrop-fx + 截图）——可开，但必须修过

发黑根因（已修）：

1. **双 WebGL**：GPU 雨丝 + raindrop 抢上下文 → 贴屏时**关掉 GPU 雨丝**，雨丝画进折射底图（CPU）
2. **截到黑 WebGL**：html2canvas **忽略全部 canvas**，勿 `drawImage` WebGL
3. **层级**：玻璃 canvas 挂 `site-fx__rain-glass` 盖 UI，不是 `site-bg`
4. **mist**：始终 `mist: false`，近黑 mistColor 禁用
5. **过黑探测**：截图像素过暗 → **自动降级**到 `glass-drops.js` 2D 珠

仍勿：`backgroundColor: null` 透明进 WebGL；默认库 mist。

## 其它「更真」路线

| 方案 | 说明 |
|------|------|
| SVG `backdrop-filter: url(位移图)` | 活 DOM 折射，Chromium 好；Safari 弱 |
| Radiant / atmos-fx | 观感参考；整页活 UI 仍要底图或位移图 |
| 仅 2D `glass-drops` | 回退路径，无真折射文字发糊 |

## 验收

- 手机/桌面：水珠内能看到 UI/文字弯折，非整屏黑点灰罩
- 失败时自动回退 2D，页面仍可用
- 白底头像上仍是透亮水，不是墨点
