---
description: 大雨/暴雨视觉必须以「最真实」为第一目标
globs: js/*rain*.js,js/glass-drops.js,js/storm-lightning.js,css/style.css,docs/*thunder*,docs/*rain*
alwaysApply: false
---

# 雨效：最真实优先

## 硬性目标

对齐小米天气「雨水打在玻璃屏上」。真实度 > 性能，但 **文字尽量可读**。

## 贴屏折射（storm）

用户确认要 **半透明大折射珠**（raindrop-fx + html2canvas），略暗可接受。

- **storm + html2canvas + RaindropFX** → 默认开贴屏真折射
- 冷凝微珠（droplets）**少**；主看大 spawn 珠
- 截图 `brightness(1.2)` + CSS `brightness(1.12)` 缓解发暗
- mist / backgroundBlur 保持关；过黑探测失败 → demote 回 2D

## 已证伪 / 勿再做

- 黑底画布 + CSS `mix-blend-mode: screen`（手机失效整页黑）
- 默认 mist 近黑；WebGL 雨丝 drawImage 进 2D 折射底图；大软灰喷雾球
- 密到糊罩的冷凝点阵

## 回退路径（demote / 非 storm）

1. GPU / 2D 短密雨丝在 `site-bg`
2. `glass-drops.js`：透明亮珠 + 下滑湿痕；**冷凝少**
3. 背景雾很淡

## 验收

- 大水滴半透明、有折射体积感（贴屏成功时）
- 冷凝珠明显少于「满屏霜点」
- 比旧贴屏版略亮；过黑则自动回退 2D
