---
description: 大雨/暴雨视觉必须以「最真实」为第一目标
globs: js/*rain*.js,js/glass-drops.js,js/storm-lightning.js,css/style.css,docs/*thunder*,docs/*rain*
alwaysApply: false
---

# 雨效：最真实优先

## 硬性目标

- 大雨 / 暴雨要对齐 **小米天气「雨水打在玻璃屏上」**：短密雨丝 + 贴屏水珠/泪痕，观感真实。
- **真实度 > 性能**（尤其 `?rain=storm`）；大雨可略轻量，但不得故意用「糊球 / 黑点 / 灰雾罩」交差。
- 参考素材：用户提供的小米天气大雨录屏（整屏湿玻璃盖在 UI 上，水珠有高光与轻微折射感）。

## 禁止再次踩坑

- **不要**再把 `raindrop-fx` 以不透明全屏层盖在 UI 上 + `html2canvas` 硬截图这条路直接上线手机——已验证会出 **整屏黑点 / 灰蒙 / 发黑**。
- **不要**打开 raindrop 默认 `mist` + 近黑 `mistColor`（库默认接近不透明黑雾）。
- **不要**把 WebGL 雨丝 `drawImage` 进 2D 折射底图（预乘透明会整屏发黑）。
- **不要**用大量软喷雾微珠（`noSpray` 关掉的那种）冒充实玻璃。

## 当前可靠做法（直到有更稳的真折射）

1. **背景**：GPU / 2D 雨丝 + 冷蓝天空（`site-bg`）。
2. **贴屏**：`js/glass-drops.js` 清晰滑动珠 + 泪痕（`noSpray: true`），盖在 UI 上；精灵偏亮、有高光，忌发黑团。
3. **`raindrop-fx`**：仅作 **无 GPU 时的背景层兜底**，且必须 `mist: false`、`dropletsPerSeconds: 0`；勿与 GPU 雨丝双 WebGL 抢上下文。
4. 若再做「真折射贴屏」：必须先解决双 WebGL、底图像素不发黑、手机验证通过，再合入。

## 验收直觉

- 白底头像上应是 **透亮水珠**，不是墨点。
- 文字仍可读，没有一层脏灰罩。
- 暴雨可比大雨更密、更湿，但仍是玻璃水，不是噪点滤镜。
