[project]
name = "shigure-rain-analysis"
version = "0.1.0"
description = "雨效对照、截图分析与 FPS 基准（本地 uv 环境，勿装 C 盘）"
requires-python = ">=3.11"
dependencies = [
  "numpy>=2.0",
  "pillow>=11.0",
  "opencv-python-headless>=4.10",
  "scipy>=1.14",
  "playwright>=1.49",
]

[tool.uv]
managed = true
